# The TV app currently relies on Retrofit's generated reflection metadata and
# kotlinx.serialization's generated serializers. Keep this file as the single
# place for future release-only rules as the domain models grow.
