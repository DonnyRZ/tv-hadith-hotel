# Docker configuration

Docker Compose currently provisions only the local PostgreSQL, MinIO, and
optional monitoring services. Application Dockerfiles will be added after the
backend and frontend implementation phases begin.
